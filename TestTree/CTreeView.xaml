<UserControl x:Class="TestTree.CTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestTree"
             mc:Ignorable="d"
             Name="cTreeView"
             Loaded="cTreeView_Loaded"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <HierarchicalDataTemplate
            DataType="{x:Type local:Node}"
            ItemsSource="{Binding Path=Items}"
            x:Key="TheTreeTemplate">
            <StackPanel Orientation="Horizontal">
                <CheckBox IsChecked="{Binding Path=IsSelected}" />
                <TextBlock Text="{Binding Path=Name}" />
            </StackPanel>
        </HierarchicalDataTemplate>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="{Binding Test, ElementName=cTreeView}"></TextBlock>
        <TreeView Name="TheTree"
            ItemsSource="{Binding ItemsSource, ElementName=cTreeView}"
            ItemTemplate="{StaticResource TheTreeTemplate}"></TreeView>
    </StackPanel>
</UserControl>
